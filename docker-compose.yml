services:
  streamcam:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streamcam-app
    ports:
      - "${PORT}:3001"
    environment:
      - NODE_ENV=production

    restart: unless-stopped
    command: npm start
